name:  Deploy

on:
  workflow_dispatch: 
  
jobs:
  deploy:
    runs-on: windows-latest
    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v5
        with:
          name: .net-app

     
