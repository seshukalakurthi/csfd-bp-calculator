name:  Deploy

on:
  workflow_run:
    workflows: ["Basic Build"]   # name of the CI workflow above
    types:
      - completed
  
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: bp
          path: ./bp
